js是动态类型语言， 对一个变量赋值并不会考虑它的类型，无需进行类型检测， 可以尝试调用任何对象的任何方法，而不用考虑原型是否出错
 这种思想思想在设计模式种被称为鸭子类型（如果一个动物走路像鸭子，叫也像鸭子，那它就是鸭子）

 设计模式的定义：在面向对象软件设计过程中针对特定问题的简洁而优雅的解决方案

 1.单例模型：
     保证一个类只有一个实例，并提供一个全局访问点， 实现的方法为先判断实例存在与否， 存在直接返回， 不存在创建在返回

 2.策略模式：
    定义一系列算法， 把他们封装起来， 并且使他们可以相互替换

 3.代理模式：
    为一个对象提供一个代用品或者占位符， 以便控制对他的访问

 4.中介者模式：
     通过一个中介者对象，其他所有的对象都通过中介者对象来通信，而不是互相引用，当其中的一个对象发生改变时，只需要通知中介者对象即可。
     通过中介者模式可以解除对象与对象之间的紧耦合关系。

5.工厂模式：
   工厂模式是为了解决多个类似对象声明的问题;也就是为了解决实列化对象产生重复的问题。