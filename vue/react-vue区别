相同点：
   1.都使用了虚拟dom，（一个映射真是dom的js对象）
   2.都提供了响应式和组件化的属兔组件
   3.都将注意力保存在了核心库， 而将其他功能如路由和全局状态管理交给其余库，

不同点：
    1.react中，当组件的状态发生变化时，他回会以该组件为根，重现渲染整个整个组件子树。
       而在vue中，在getter实现依赖收集，组建的依赖是在渲染的过程中自动追踪的，系统能准确知道那个组件确实需要重新渲染。
    2.vue可以通过v-model，双向绑定数据，而react只能单向
    3.vue中diff算法：
         vue对比节点，当节点类型相同，但是classname不同，认为是不同类型的元素， 删除重建
          而react会认为是同类型节点， 只是修改节点属性。
          vue在对比列表，采用从两端到中间的对比方式， 而reat，则采用从左到右方式， 当一个集合把最后的
          一个节点移动的到第一个，react会把前面的节点依次移动， 而vue会把最后的一个节点移动到第一个。
    4.：vue的整体思想仍然是拥抱经典的html(结构)+css(表现)+js(行为)的形式，vue鼓励开发者使用template模板，
         react整体上是函数式的思想，组件使用jsx语法，all in js，将html与css全都融入javaScript，jsx语法相对来说更加灵活，
          当组件重现渲染，子组件也会重现渲染
    5.这也就导致，我们在react引入组件不用在声明，而在vue中要通过components声明：
         是因为react中的render函数支持闭包属性，所以可以直接调用 ，但是在vue中使用的数据必须在中转一次， 
    6.vuex和redux的区别：
        redux使用的是不可变数据，redux每次用新的state替换旧的state，
        但是vuex是直接修改。
    7.vue有插槽，react没有插槽
       
        
 
