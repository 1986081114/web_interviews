如何保证前端代码安全性：
    如果是一些类似于web页面的轮播图什么的的不需要保护，但是如果是js控制重要功能， 交互逻辑，支付等需要加密

    加密方法：
       1.打包压缩， 在某种程度上让代码的可读性差，
       2.混淆加密：
         js不可能有不可逆转的加密， 因为代码在浏览器执行时， 必须转解密还原成原始代码， 才能被js引擎识别运行。
         代码混淆：代码混淆是将代码转换成一种功能上等价，但是难于阅读和理解的形式。代码混淆可通过将代码中的各种元素改写成无意义的名字；
          重写代码中的部分逻辑，将其变成功能上等价，但是更难理解的形式；

        js混淆器大致有两种:
            通过正则替换实现的混淆器
            通过语法树替换实现的混淆器
        一种实现成本低，但是效果也一般，适合对混淆要求不高的场景。第二种实现成本较高，但是更灵活，而且更安全，更适合对抗场景