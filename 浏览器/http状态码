1xxL信息性状态码： 表示响应已被服务器接收了，正在处理 该状态码往往是临时性的，因为马上会更改 
2xx：请求成功请求正常处理完毕
    200：成功， 标准的成功。get请求中响应将包含与请求对应的实体，post请求中，响应包含实体的描述或者操作结果
    202，已接受请求accept，但还没有处理，可能会成功，或不会被执行，或禁止执行
    204：无内容： 服务器成功处理了请求，但没有返回任何内容
    205重置内容： 服务器成功处理请求， 但没有返回内容，区别204的是，该响应要求客户端浏览器界面更新
    206部分内容：服务器成功处理了请求的部分内容，一般用于http下载工具的断点续传功能
3xx：重定向状态码： 需要进行附加操作完成请求
     301：永久性重定向： 请求资源的路径已被永久性的转移到了新位置，并且将来对此资源的引用都应该使用它返回的URL
     302：临时性重定向： 请求资源被分配新的url，希望本次请求使用新的url访问
     303： 请求对应的资源存在另一个URL，应使用get请求获取请求的资源，与302区别是表示客户端应采用get方法获取资源
     304：未修改，表示客户端发送附加条件的请求时，服务器允许请求访问资源， 但为满足条件的情况，如if-MODIFIED-SINCE和ifNone-Match
     307;临时性重定向：与302相同，尽管302标砖禁止post变成get，但实际使用大家不会遵守，307会严格按照标准，不会讲post变成get
     
4xx：客户端错误状态吗： 服务器无法完成处理请求、
     400：请求错误：请求报文中存在语法错误，需要修改
     401未授权：表示发送的请求需要通过http认证
     403：拒绝：表示已经认证了请求资源的访问被拒绝，服务端没有给出相信理由，在主体可以看到，
     404：未找到资源：无法找到请求资源
     408：请求超时： 客户端没有在服务器预备时间内完成一个请求的发送
     409：冲突：如多人操作
5xx：服务器错误状态码， 服务器请求出错。
 500：内部服务器错误：
 501：请求无法实现： 不支持请求的某个功能
 502::作为网关或者代理工作的服务器尝试执行请求时，从上游服务器接收到无效的响应。  
 503：表示服务器不可用： 表明服务器暂时处于超负荷或正在维护， 无法处理请求
 504：作为网关或者代理工作的服务器尝试执行请求时，未能及时从上游服务器收到响应。 
 
数组对象去重

计算机网络七层模型：
  应用层
  表示层
  会话层
  传输层
  网络层
  数据链路层
  物理层

  五层模型：
   应用层:直接为应用程序提供服务， 应用层协议定义的是应进程通讯和交互的规则，不用的应用有着不同的应用层协议， 如http（万维网协议）， FTP协议（文件传输），smtp（电子邮件）
   传输层： 传输层 也成为运输层， 负责为两台主机中的进程提供通讯服务，应用控制协议tcp， 用户传输协议（udp）
   网络层： 负责为两台主机提供通讯服务， 并通过选择合适的路由传递数据到目标主机
   数据链路层： 负责将网络层的ip数据封装成帧，
   物理层： 确保数据可以在各种物理媒介进行传输

   应用层： 
     我们使用易于理解的域名访问互联网， 但是传输层幼的是ip地址， 因此需要使用域名系统DNS来进行域名与ip地址之间的转换
      DNS域名是一个逻辑性的概念，根域名和顶级域名，顶级域名， 二级域名，三级域名 
          先从本地dns查找，不存在就像根域名去查找，根域名服务器返回负责 .com 的 顶级域名 服务器的 IP 地址的列表。
          然后本地 DNS 服务器再向其中一个负 责 .com 的顶级域名服务器发送一个请求，负责 .com 的顶级域名服务器返回
           负责 .baidu 的权威域名服务器的 IP 地址列表
           递归查询和迭代查询
            递归查询指的是请求发送后，域名服务器为向下一级服务器发送请求， 最后向用户返回查询的结果，用户只需要发送一次请求，
            迭代查询， 查询请求后，域名服务器返回单次查询结果，下一级的查询有用户自己请求
            一般来说，我们像本地dns服务器发送请求的方式是递归查询， 因此我们只发送一次请求， 然后，本地dns返回最终的结果
            本地dns服务器像其他域名服务器请求的过程的迭代查询， 因为每一次域名服务器只返回单词查询结果， 下一级的查询由dns服务器自己进行
        递归：客户端只发一次请求，要求对方给出最终结果。

        迭代：客户端发出一次请求，对方如果没有授权回答，它就会返回一个能解答这个查询的其它名称服务器列表，
                  客户端会再向返回的列表中发出请求，直到找到最终负责所查域名的名称服务器，从它得到最终结果。
        授权回答：向dns服务器查询一个域名，刚好这个域名是本服务器负责，返回的结果就是授权回答。
        从递归和迭代查询可以看出：
        客户端-本地dns服务端：这部分属于递归查询。
        本地dns服务端---外网：这部分属于迭代查询。

      DHCP动态主机配置协议：动态分配ip地址， 路由器不开启这个功能， 连接路由器的电脑就要手动配置ip
     
传输层：
  传输层负责为两台主机中的进程提供通信服务。

网络层：
 ARP根据ip地址找到mac地址

    