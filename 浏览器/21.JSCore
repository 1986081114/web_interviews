浏览器内核的模块主要由渲染引擎和js引擎组成，期中jscore就是一种js引擎，
一般是将js引擎进行封装，提供一套js运行环境以及native与js数据类型之间转换桥梁，常用于oc和js代码之间的相互调用，这意味着他
可以脱离渲染单独去执行js。
在native应用中可以开启多个线程异步执行不同的需求，意味着可以创建多个js虚拟机，互相隔离不影响，可以并行地执行不同的js任务。
在js虚拟机中可以关联多个js执行上下文，并通过JSValue（值对象）来和native进行数据传递通信，同时可以通过JSExport (协议)将
native解析的类的方法和属性转换为js的接口供其调用。

js