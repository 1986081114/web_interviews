http1.0
  每个tcp连接只能发送一个请求， 发送数据完毕后， 链接就关闭， 如果还要i请求必须建立新的连接，
  http是一种无状态，无连接的请求， 对于发送或者响应的数据都不做持久化处理。

http1.1
   虽然也是无状态的， 但为了实现保持状态功能， 引入cookie技术， 有了cookie，和http协议通信，就可以管理状态了，
   可以持久化链接，tcp默认不关闭， 可以被多个请求复用，只有在一段时间内没有请求自动关闭

http2.0：客户端和服务器均使用  
  二进制技术， 不再是纯文本， 支持一个tcp链接发送多个请求， 利用HPACK压缩头部，减少数据传输量。
  允许服务端主动推送数据。

http1.1和http2.2：
  1.头部压缩： http2.0会压缩头部， 如果同时发送多个请求，他们的头部是相似的，那么协议会帮忙消除重复的部分。
  2.二进制格式： http2.0不再是纯文本报文，而是采用了二进制报文。计算机收到二进制报文，无需转化，提高效率。
  3.数据流： http2的数据包不是按照顺序发送的， 因此需要对数据包做标记， 指出属于哪个回应
  4.多路复用：http2.2可以在一个链接并发多个请求或回应， 不用按照顺序， 
            移除了http1.0中的穿行请求， 不用等待，也不会出现对头阻塞的问题，提高连接利用率。
  5.：服务器推送：在浏览器请求html的时候，会提前把可能用到的js css文件主动发送给客户端。

 	HTTP/1.1 中的管道（ pipeline）传输中如果有一个请求阻塞了，那么队列后请求也统统被阻塞住了
•	HTTP/2 多请求复用一个TCP连接，一旦发生丢包，就会阻塞住所有的 HTTP 请求。

